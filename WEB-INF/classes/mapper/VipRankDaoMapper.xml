<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.vip.dao.VipRankDao">
    <select id="select"  resultType="com.zs.vip.entity.VipRankEntity">
        SELECT  * FROM vip_rank WHERE ${key}=#{value}
    </select>

</mapper>